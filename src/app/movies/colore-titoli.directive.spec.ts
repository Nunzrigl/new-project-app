import { ColoreTitoliDirective } from './colore-titoli.directive';

describe('ColoreTitoliDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoreTitoliDirective();
    expect(directive).toBeTruthy();
  });
});
